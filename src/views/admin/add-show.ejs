<%- include('../partials/header', { title: title, currentPage: currentPage }) %>

<div class="container">
    <div class="form-container">
        <div class="form-header">
            <h1>Add New Show/Movie</h1>
            <p>Fill in the details below</p>
        </div>

        <% if (errors && errors.length > 0) { %>
            <div class="form-error">
                <ul>
                    <% errors.forEach(error => { %>
                        <li><%= error.msg %></li>
                    <% }) %>
                </ul>
            </div>
        <% } %>

        <form action="/admin/add-show" method="POST">
            <div class="form-group">
                <label for="name">Name *</label>
                <input
                    type="text"
                    id="name"
                    name="name"
                    value="<%= formData.name || '' %>"
                    required
                >
            </div>

            <div class="form-group">
                <label for="mediaType">Type *</label>
                <select id="mediaType" name="mediaType" required style="width: 100%; padding: 0.8rem; background: #242938; border: 2px solid #2a3040; color: #e4e6eb; font-size: 1rem;">
                    <option value="movie" <%= formData.mediaType === 'movie' ? 'selected' : '' %>>Movie</option>
                    <option value="tv-show" <%= formData.mediaType === 'tv-show' ? 'selected' : '' %>>TV Show</option>
                </select>
            </div>

            <div class="form-group">
                <label for="startDate">Start Year *</label>
                <input
                    type="number"
                    id="startDate"
                    name="startDate"
                    min="1900"
                    max="2100"
                    value="<%= formData.startDate || '' %>"
                    required
                >
            </div>

            <div class="form-group">
                <label for="endDate">End Year (Optional)</label>
                <input
                    type="number"
                    id="endDate"
                    name="endDate"
                    min="1900"
                    max="2100"
                    value="<%= formData.endDate || '' %>"
                >
                <small style="color: #b4b8c5; display: block; margin-top: 0.5rem;">
                    Leave empty for ongoing TV shows or same as start year for movies
                </small>
            </div>

            <div class="form-group">
                <label for="imageUrl">Image URL (Optional)</label>
                <input
                    type="text"
                    id="imageUrl"
                    name="imageUrl"
                    value="<%= formData.imageUrl || '' %>"
                    placeholder="/images/example.jpg"
                >
            </div>

            <div class="form-group">
                <label for="genres">Genres (comma-separated)</label>
                <input
                    type="text"
                    id="genres"
                    name="genres"
                    value="<%= formData.genres || '' %>"
                    placeholder="Action, Drama, Sci-Fi"
                >
                <small style="color: #b4b8c5; display: block; margin-top: 0.5rem;">
                    Enter genres separated by commas
                </small>
            </div>

            <button type="submit" class="btn-submit">Add Show</button>
        </form>

        <div class="form-footer">
            <p>
                <a href="/admin">Cancel and go back to admin panel</a>
            </p>
        </div>
    </div>
</div>

<%- include('../partials/footer') %>
